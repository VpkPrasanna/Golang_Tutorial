package main

// func main() {

// this set of lines take input of one word or one number if there is more than one word or number ot will throw error
// var myString string
// fmt.Scanln(&myString)
// fmt.Println(myString)

// if you want to store the printed value in any varible it will throw an error to treat this error we are using _ varible seperately
// var name string = "hitesh"
// var a, _ = fmt.Println(name)
// fmt.Println(a)
// fmt.Println(b)

// := is called as short variable declarations
// reader := bufio.NewReader(os.Stdin)
// fmt.Print("Enter you full name: ")
// myname, b := reader.ReadString('\n')
// fmt.Println(myname)
// fmt.Println(b)

// incase you need add 2 to the input rating you need to do like this so that golang will accept this pattern
// reader := bufio.NewReader(os.Stdin)
// fmt.Print("Enter your rating: ")
// myrating, _ := reader.ReadString('\n')
// mynumRating, _ := strconv.ParseFloat(strings.TrimSpace(myrating), 64)
// fmt.Println(mynumRating + 2)

// }
